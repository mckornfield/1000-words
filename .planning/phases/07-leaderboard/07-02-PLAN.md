---
phase: 07-leaderboard
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/app/src/lib/router.ts
  - packages/app/src/App.tsx
autonomous: true
requirements:
  - "Leaderboard page accessible from dashboard navigation"
  - "Follows existing supabase*Repository + mock*Repository pattern in data/ layer"
must_haves:
  truths:
    - "The string '/leaderboard' is a member of the RoutePath union type in router.ts"
    - "parseRoute() returns path '/leaderboard' when the URL pathname starts with 'leaderboard'"
    - "getParentRoute('/leaderboard') returns '/dashboard'"
    - "getRouteBreadcrumbLabel('/leaderboard') returns 'Leaderboard'"
    - "App.tsx renderPage() handles case '/leaderboard' and renders LeaderboardPage"
    - "leaderboardRepo is injected into AppContextValue in both isDemo and production branches"
  artifacts:
    - path: "packages/app/src/lib/router.ts"
      provides: "'/leaderboard' in RoutePath union, parseRoute case, getParentRoute case, breadcrumb label"
      contains: "/leaderboard"
    - path: "packages/app/src/App.tsx"
      provides: "LeaderboardPage import, repo injections, renderPage case"
      contains: "LeaderboardPage"
  key_links:
    - from: "App.tsx renderPage()"
      to: "packages/app/src/features/leaderboard/LeaderboardPage.tsx"
      via: "case '/leaderboard': return <LeaderboardPage dashboardData={data} />"
      pattern: "LeaderboardPage"
    - from: "App.tsx appContextValue useMemo"
      to: "packages/app/src/data/leaderboard/supabaseLeaderboardRepository.ts"
      via: "leaderboardRepo: createSupabaseLeaderboardRepository()"
      pattern: "createSupabaseLeaderboardRepository"
---

<objective>
Wire the /leaderboard route into the router and inject the leaderboard repository into AppContextValue for both demo and production modes.

Purpose: Without this wiring, navigating to /leaderboard falls through to the default case (DashboardPage) and leaderboardRepo is undefined in the context, causing a runtime crash in plan 03.

Output: router.ts with /leaderboard in RoutePath union + parseRoute/getParentRoute/breadcrumb support; App.tsx with LeaderboardPage import + repo injection + renderPage case.

Decision coverage: D-08 (no new NavBar tab — only route registration and App.tsx wiring), D-09 (getParentRoute returns /dashboard enabling the back button).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-leaderboard/07-CONTEXT.md
@packages/app/src/lib/router.ts
@packages/app/src/App.tsx
@.planning/phases/07-leaderboard/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /leaderboard to router.ts (RoutePath, parseRoute, getParentRoute, breadcrumb)</name>
  <files>packages/app/src/lib/router.ts</files>
  <read_first>
    - packages/app/src/lib/router.ts — read the full file; note the RoutePath union, parseRoute() switch pattern, getParentRoute() pattern, getRouteBreadcrumbLabel() switch pattern
  </read_first>
  <action>
    Make four targeted edits to packages/app/src/lib/router.ts:

    1. RoutePath union: add "/leaderboard" as a new member. Insert it after "/objectives/:objectiveId" (keep alphabetical-ish order consistent with current file order).

    2. parseRoute(): add a new if-block before the final "Unrecognized path" fallback:
       if (segments[0] === "leaderboard") {
         return { path: "/leaderboard", params: {} };
       }

    3. getParentRoute(): add a case before "if (currentRoute === '/dashboard') return null;":
       if (currentRoute === "/leaderboard") return "/dashboard";

    4. getRouteBreadcrumbLabel(): add a case in the switch:
       case "/leaderboard": return "Leaderboard";

    Also update the JSDoc comment at the top of the file to list the new route: add "- /leaderboard (leaderboard rankings)" in the Supported route patterns list.

    Do NOT modify NavBar.tsx — per D-08 leaderboard has no bottom nav tab.
  </action>
  <verify>
    <automated>pnpm --filter @1000words/app tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - "/leaderboard" is in the RoutePath union
    - parseRoute() returns { path: "/leaderboard", params: {} } for "leaderboard" segment
    - getParentRoute("/leaderboard") returns "/dashboard"
    - getRouteBreadcrumbLabel("/leaderboard") returns "Leaderboard"
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire LeaderboardPage + repos into App.tsx</name>
  <files>packages/app/src/App.tsx</files>
  <read_first>
    - packages/app/src/App.tsx — read the full file to see the exact import block, appContextValue useMemo (both isDemo and prod branches), and renderPage() switch
    - packages/app/src/data/leaderboard/supabaseLeaderboardRepository.ts — verify the exact exported function name
    - packages/app/src/data/leaderboard/mockLeaderboardRepository.ts — verify the exact exported function name and its parameter (currentUserId)
  </read_first>
  <action>
    Make three targeted changes to packages/app/src/App.tsx:

    1. Import block — add three new import lines after the existing stats repository imports (keep grouped by feature):
       import { createSupabaseLeaderboardRepository } from "./data/leaderboard/supabaseLeaderboardRepository";
       import { createMockLeaderboardRepository } from "./data/leaderboard/mockLeaderboardRepository";
       import { LeaderboardPage } from "./features/leaderboard/LeaderboardPage";

    2. appContextValue useMemo — isDemo branch: add leaderboardRepo: createMockLeaderboardRepository(session.userId) as the last field before the closing brace. The mock factory takes the current user's ID so it can flag that user's entry.

    3. appContextValue useMemo — production branch: add leaderboardRepo: createSupabaseLeaderboardRepository() as the last field before the closing brace.

    4. renderPage() switch: add a new case before the default:
       case "/leaderboard":
         return <LeaderboardPage dashboardData={data} />;

    The LeaderboardPage component will be created in plan 03. At the time this plan runs, the import will cause a TypeScript error if the file does not exist yet. To avoid blocking: create a stub file packages/app/src/features/leaderboard/LeaderboardPage.tsx with a minimal export:
       export function LeaderboardPage(_props: { dashboardData: unknown }) { return null; }
    Plan 03 will replace this stub with the full implementation. Document the stub with a comment: "Stub — replaced by plan 07-03".

    This ensures App.tsx compiles in this plan's wave without waiting for plan 03.
  </action>
  <verify>
    <automated>pnpm --filter @1000words/app tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - App.tsx imports createSupabaseLeaderboardRepository, createMockLeaderboardRepository, and LeaderboardPage
    - Both isDemo and prod branches of appContextValue include leaderboardRepo
    - renderPage() case "/leaderboard" renders LeaderboardPage with dashboardData prop
    - Stub LeaderboardPage.tsx exists in packages/app/src/features/leaderboard/
    - TypeScript compiles without errors
    - pnpm review passes (lint + typecheck + test + build)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client URL → parseRoute() | User can manually navigate to any URL; parseRoute must not expose internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Elevation of Privilege | /leaderboard route | accept | requiresAuth("/leaderboard") returns true (all non-login routes require auth — verified in router.ts line 171-173). No special elevation needed. |
| T-07-06 | Tampering | AppContextValue leaderboardRepo injection | accept | Repos are injected server-side in useMemo; no user-controlled input can alter which repo implementation is selected (isDemo is derived from env var at build time). |
</threat_model>

<verification>
```bash
# Route registered
grep -c '"/leaderboard"' packages/app/src/lib/router.ts

# Repo wired in both branches
grep -c "leaderboardRepo" packages/app/src/App.tsx

# renderPage case present
grep -c "LeaderboardPage" packages/app/src/App.tsx

# Full quality gate
pnpm review 2>&1 | tail -20
```
</verification>

<success_criteria>
- "/leaderboard" is in RoutePath union, parseRoute handles it, getParentRoute returns "/dashboard", breadcrumb returns "Leaderboard"
- App.tsx has LeaderboardPage imported, leaderboardRepo in both appContextValue branches, and renderPage case for /leaderboard
- Stub LeaderboardPage.tsx exists and compiles
- pnpm review passes (all gates)
</success_criteria>

<output>
Create .planning/phases/07-leaderboard/07-02-SUMMARY.md when done
</output>
